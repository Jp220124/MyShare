# Cloudflare R2 Configuration (optional)
# If not configured, the app will fall back to external services
VITE_R2_WORKER_URL=https://your-worker.workers.dev

# To set up R2:
# 1. Create a Cloudflare account at https://dash.cloudflare.com
# 2. Go to R2 and create a bucket named "webshare-files"
# 3. Create a KV namespace for file metadata
# 4. Deploy the worker from cloudflare-worker/r2-worker.js
# 5. Update the wrangler.toml with your KV namespace IDs
# 6. Run: npx wrangler deploy
# 7. Copy your worker URL here
